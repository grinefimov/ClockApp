<UserControl x:Class="ClockApp.TimerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClockApp"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <materialDesign:Card Height="50" Margin="0,1,0,0">
            <Grid>
                <TextBlock Style="{StaticResource MaterialDesignSubheadingTextBlock}" VerticalAlignment="Center"
                           Margin="20,0,0,0" Foreground="White">
                    <TextBlock Text="{Binding Number}"/>
                </TextBlock>
                <materialDesign:TimePicker Is24Hours="True" WithSeconds="True" Width="100" FontSize="20" Margin="0,0,145,0" Foreground="White"
                                           SelectedTime="{Binding SelectedTime}"/>
                <Button Content="Start" Width="80" Margin="55,0,0,0"/>
                <Button Content="Reset" Width="80" Margin="235,0,0,0" IsEnabled="False" />
                <!--<Button Style="{StaticResource MaterialDesignFloatingActionMiniButton}" Height="20" Width="20" Margin="305,0,0,0">
                    <materialDesign:PackIcon Kind="CloseOutline" Height="15" Width="15"/>
                </Button>-->
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
